var ultralite = require('../lib/ultralite.js');

describe('character', function() {
	describe('attributes', function() {
		it("must have a Strength (ST) attribute")
		it("must have a Dexterity (DX) attribute")
		it("must have an Intelligence (IQ) attribute")
		it("must have a Health (HT) attribute")
	});

	describe('skills', function() {

	});

	describe('advantages', function() {

	});
});