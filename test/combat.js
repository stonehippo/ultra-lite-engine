var ultralite = require('../lib/ultralite.js');

describe('combat', function() {
	describe('attack', function() {
		it("should allow a character to attack")
		it("should allow a character to pick a manuever")
		it("should determine the success or failure of an attack")
	});

	describe('damage', function() {
		it("should allow a character to determine damage for an attack")
	});
});