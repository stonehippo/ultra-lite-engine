import test from 'tape';
import ultralite from '../lib/ultralite.js'

test('--- combat module tests ---', assert => {
    assert.skip("should allow a character to attack");
    assert.skip("should allow a character to pick a manuever");
    assert.skip("should determine the success or failure of an attack");
    assert.skip("should allow a character to determine damage for an attack");
    
    assert.end();
});